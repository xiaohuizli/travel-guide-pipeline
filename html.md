# 阶段三：本地 HTML

可选 [frontend-design](https://skills.sh/anthropics/skills/frontend-design) 辅助视觉设计；验收优先使用当前已提供的浏览器工具，已有 playwright 技能时按其说明操作。无需 Sites、托管、数据库或前端框架。

## 实现要求

- 优先单文件 HTML：UTF-8、语义化标题与导航、内嵌 CSS/JS，双击可读。资料中的文本正确转义，不把网页返回的脚本和任意 HTML 直接注入产物。
- 使用用户参考的色彩、字体比例、图片构图与内容密度，不复制其中私人行程和旧价格。没有参考时采用适合长文阅读的清晰版式，不强制某种风格。
- 图片可按工具与来源允许的方式嵌入；压缩至适合显示的大小。字体仅使用有使用权的文件或系统字体。不默认依赖 CDN。文件过大时改资源目录并附 zip，说明不能只移动 HTML。
- 手机单列阅读，表格允许局部横向滚动；长链接换行，不把整页缩成桌面版。
- 目录锚点和来源编号可跳转；每张图有 alt、说明和来源。
- 清单使用真实 checkbox 和独立 localStorage key（每份攻略唯一）；try/catch 处理 file:// 或浏览器限制，存储失效时保持当前页可勾选并显示“不支持持久保存”。勾选不代表出票成功。
- 地图按需加载：离线 SVG 展示旅行顺序，标明非按比例导航；在线底图明确标示联网，用户点击后才请求，不影响正文离线阅读。不可用时保留静态路线。底图署名与坐标系按实际服务处理；没有合法可用接口时不假装具备导航。
- 打印用 @page 和 @media print；隐藏交互工具、避免表头丢失、孤立标题和卡片截断。用正常文档流分页，不以固定高度 overflow:hidden 掩盖超页。不为追求固定页数删除内容。

## 必需 QA

1. 读取所有本地资源，检查破图、重复 id、失效内部锚点与未替换的占位内容。
2. 浏览器检查桌面和手机宽度：文字、表格、长链接、图片不遮挡或造成整页横向溢出。
3. 测试目录跳转、来源链接目标、清单勾选与刷新恢复；验证禁用存储后的降级。如有地图，检查未点击时不加载底图及失败时的静态备选。
4. 检查断网时正文与必要图片可读。不要仅因代码内嵌就声明离线验证通过。
5. 检查打印预览/渲染页，修复截字、缺失、过度留白；打印渲染只作 QA 时留在 work。用户明确要 PDF 才额外交付，并使用可用 PDF 技能检查。
6. 对照 guide.md 检查内容完整与预算/日期一致。报告实际检查结果；浏览器不可用时明确“未完成视觉验收”，不要把静态检查称为全部通过。

完整链路交付命名清晰的 HTML 和必要资源包。只交付本地文件不触发上线、发消息或订票。
